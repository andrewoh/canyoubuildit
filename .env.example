# Set this in production. For the requested private page, the password is babymoon.
DASHBOARD_PASSWORD=babymoon
SESSION_SECRET=replace-with-a-long-random-secret
NEXT_PUBLIC_SITE_URL=https://seoul.canyoubuildit.com
IMPORT_SECRET=replace-with-a-long-random-import-secret
# Required in production if using POST /api/import/transactions for durable imports.
BLOB_READ_WRITE_TOKEN=
TRANSACTION_IMPORT_BLOB_PATH=seoul/imported-transactions.json
